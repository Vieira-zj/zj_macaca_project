/**
 * Include common UI objects selector invoked by UI tasks.
 * 
 */

const baiduMainPage = {
  selectorLoginLink: 'div#u1 > a[name=tj_login]',
};

const loginDialog = {
  selectorDialogMain: 'div#passport-login-pop-dialog',
};

module.exports = {
  baiduMainPage,
  loginDialog,
}